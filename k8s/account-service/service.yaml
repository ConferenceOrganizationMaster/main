apiVersion: v1
kind: Service
metadata:
  name: account-service-service
  namespace: default
spec:
  selector:
    app: account-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP